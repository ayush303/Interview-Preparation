package LLD.finance_and_paymentSystem.onlineStockBroker.solutions.enums;

/**
 * LIMIT — trade only at the specified price or better.
 * MARKET — trade immediately at the best available price.
 */
public enum OrderType {
    MARKET,
    LIMIT
}
